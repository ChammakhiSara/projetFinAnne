import { FiltreStockLocalePipe } from './filtre-stock-locale.pipe';

describe('FiltreStockLocalePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreStockLocalePipe();
    expect(pipe).toBeTruthy();
  });
});
