import { FiltreBomPipe } from './filtre-bom.pipe';

describe('FiltreBomPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreBomPipe();
    expect(pipe).toBeTruthy();
  });
});
